# CC-Decisions — Running Ledger

docs/cc-decisions-active.md | v1.0 | July 2026 | CONFIDENTIAL

Governing rule: **CLAUDE.md Rule 46** (D-620) — every CC-decision is appended here at the
moment it is made, before the work implementing it is committed. The per-contract CodeClose
summarises this ledger; it does not replace it.

---

## How to Use This File

Append one row per CC-decision, at the moment the decision is made. Never batch at close.

Required per entry:

| Field | Content |
|---|---|
| CC-letter | The session's CC identifier, e.g. `CC-41-A`, `CC-0801-03` |
| Title | One line |
| Reasoning | Why this direction over the alternatives |
| Commit | Hash, filled in once known — `pending` until then |

Rule 46 conformance test: for every CC-letter named in a CodeClose, does a corresponding
entry exist in this file? Yes for all = pass.

---

## Coverage Boundary

**This ledger opens at Contract 41 (2026-07-30).**

CC-decisions from Contract 1 through Contract 40 — including the G-series, GA-1, and the
Contract 40 follow-ons — were recorded per-contract, not in a running ledger. They live in
the `OITrust-CodeClose-*.md` files in the repository root and in the ratified D-numbers in
`docs/decision-registry.md`. They are not restated here.

Backfilling those contracts into this ledger is an open item for Design. Until Design directs
otherwise, Rule 46's conformance test applies to Contract 41 forward only.

---

## Ledger

### Contract 41 — 2026-07-30 — ValidatorClose Document Install

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-41-A | Ledger opens at Contract 41; Contracts 1–40 not backfilled | Rule 46 arrived with no ledger in the repository. Backfilling forty contracts of CC-decisions is a substantial reconstruction with real fabrication risk, and the source records already exist in the root `OITrust-CodeClose-*.md` files and the ratified D-numbers. Opening forward and stating the boundary explicitly is honest; a silently partial backfill is not. Escalated to Design rather than resolved unilaterally. Phil confirmed 2026-07-31: no backfill. | 22251a9 |

### Contract 41 — 2026-07-31 — Home RACI card, All Pending Gates, warning scope

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-41-H | Aging marks the row rather than repainting it | CC-41-C fixed the header background but left the row treatment, which was the half still reading as nonstandard. Removed the zebra striping the All Initiatives grid does not have, and stopped the aging highlight painting the full row background at 8% amber: 19 of 25 live gates are past the 7-day threshold, so the wash covered nearly every row and the highlight carried no signal. Aging is now a 3px amber left border plus the day count in `#B87700` — deliberately not the `#F2A620` token, which is a fill colour and fails contrast as text. Also confirmed the `—` Level column is not a defect: those Initiatives predate sizing, and newer ones correctly show L2. | fa32fac |
| CC-41-G | Reuse RaciGlyphsComponent with an additive `readonly` input | Restyling glyphs inside the card would duplicate the visual language S-030 exists to prevent. Passing `busy=true` was rejected — it disables the follow button but still renders a hollow `i` on every row, implying a follow affordance the card does not offer. `readonly` defaults false, so the Initiative grid, My Initiative Status, and My Initiatives card are byte-identical. `c_provisional` is not carried by this summary: the provisional distinction needs Go to Build cast state the card does not fetch, so a Consulted stake renders solid here and dashed on the grid. Flagged rather than silently resolved. | afc7800 |
| CC-41-F | A separate discovery tool rather than a parameter on `get_my_raci` | `get_my_raci` maps letters onto a caller-supplied `cycle_ids` list; a Home card has no list to supply. Bolting discovery onto it would give one tool two opposite contracts. Excluded A from the card: an approval owed is a push obligation already served by My Actions, and repeating it here would read as a second work queue. Excluded CANCELLED per S-009 but deliberately kept COMPLETE — a just-approved Close Review is precisely what "recently completed" means. | afc7800 |
| CC-41-E | Targeted return refresh via a transient snapshot, not a full reload | "Refreshed on the one initiative only" is impossible with a plain route round trip, because navigating to the Initiative destroys the component and coming back re-runs a full load. Chose a 60-second sessionStorage snapshot of the queue plus a new optional `delivery_cycle_id` scope on `list_all_pending_gates`: on return, only the acted-on Initiative is re-queried and spliced in. TTL exists because rows for *other* Initiatives go stale and this screen's whole job is saying what is genuinely waiting. Rejected converting the screen to an S-018 right panel — it would make the refresh trivially correct, but Phil explicitly endorsed the existing navigation ("brought back to the initiative (it does)"), so that is a bigger change than asked for. Also fixed a dead Back link found while wiring this: `returnTo` was `'all-pending-gates'`, which `navigateByUrl` resolves as root, but the route is nested under `/initiatives`. | 6c3fc87 |
| CC-41-D | Submitter resolved in the existing users lookup, not a new query | `submitted_by_user_id` was already selected but never returned. Folding submitter-name resolution into the approver lookup avoids adding a query to a flow whose downstream waiting-on fixtures are FIFO-mocked — a new query would have shifted every slot after it (Rule 40). | 6c3fc87 |
| CC-41-C | Navy grid header rather than the pale reskin | The 2026-07-29 reskin aimed to match the Initiative list but used `#F7FAFC`; the Initiative grid actually uses `#12274A` with white uppercase labels. Copied the real treatment, including the `#F0F4F8` hover and `#E8F0FE` selected pair, so the two screens are genuinely the same surface. | 6c3fc87 |
| CC-41-B | Loud-on-open warnings get their own column rather than switching the other types off | D-616 wanted two loud artifact types; the Go to Build panel showed twelve. The cause was not migration 096 but the Contract 40 follow-on adding `computeArtifactWarningsByGate` to the read path, which exposed the ~10 types that have carried `gate_warning_behavior='primary_and_subsequent'` since Contract 25. Setting those to `'none'` would also have silenced their D-438 post-action warnings, which are still wanted — "is this mentioned after an action" and "is this loud before an action" are different questions. Migration 097 adds `gate_warning_on_open` (default false, so nothing is loud by inheritance) and the read path filters on it. Rejected inferring loudness from `gate_warning_through IS NOT NULL`: true of exactly these two rows today, but only by coincidence. | a92e1ff |

### Contract 42 — 2026-08-02 — AC-29 Maintenance Mode

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-42-A | Both maintenance tools are authenticated admin operations | D-635 required correcting `get_maintenance_mode`'s "NO JWT REQUIRED" header rather than accommodating it, and that is done — no `/maintenance-mode` carve-out was added, `/health` and `/tools` remain the only exceptions. Separately: the rescued `set_maintenance_mode` header claimed "Admin role only" but the file carried **no admin check at all** — any authenticated user could have taken the application down. Added the gate matching `clear_division_status_config`. The header was aspirational; the code was not. Also added an explicit "no system_config row" failure rather than reporting success on an `UPDATE` that matched nothing. | ecdba21 |
| CC-42-B | Bootstrap interception empties the route table; hiding the outlet is not enough | build-c-spec §5.2 requires the maintenance screen to suppress all routing and **attempt no auth**. An `*ngIf` on `<router-outlet>` satisfies neither: the router still matches a route, runs `AuthGuard`, and lazy-loads the feature module. Chose `APP_INITIALIZER` (the only hook that runs before initial navigation — a guard has already resolved a route, an `ngOnInit` read flashes the shell) which, when the flag is on, calls `router.resetConfig([{ path: '**', children: [] }])`. A wildcard matching everything and resolving to nothing runs no guard and loads no module. `AppComponent.ngOnInit` also returns early, so no version polling, news ticker, or profile resolution runs. | ecdba21 |
| CC-42-C | The Arch-1 read is a plain fetch, and it fails open | The authorized exception permits a direct Supabase read; it does not require the SDK. A `fetch` against PostgREST avoids constructing a second Supabase client, restoring a session, or importing `@supabase/supabase-js` into a new location — all of which would be auth-adjacent work in a path that must attempt no auth. Fail-open on any error was the harder call: a failed read resolving to "in maintenance" could lock every user out of a healthy application on a network blip, while the inverse failure (a user reaching a mid-deploy app) is exactly the pre-AC-29 status quo. Fail-open is strictly no worse than today; fail-closed introduces a new way to be down. | ecdba21 |
| CC-42-D | Migration 095 is committed unexecuted, plus RLS | Disposition: **no execution required**. The rescue note inferred the table existed; this contract confirmed it directly against the live schema — one row, created 2026-04-07, carrying 053's column. Both statements are guarded, so running it would be a no-op. It is committed for rebuild completeness (Rule 48), which is the ARCH-34 hole it closes. Added `ENABLE ROW LEVEL SECURITY` to satisfy Rule 38 — idempotent, and policy ownership stays with 031 exactly as column ownership of `status_refresh_last_run` stays with 053. | ecdba21 |
| CC-42-E | ARCH-34 answered by enumeration, not inference | Contract 42 §6 worried that `system_config` was found by accident and that others might exist. Rather than reason about it, read the live table list from the PostgREST OpenAPI document and diffed it against every `CREATE TABLE` on master: 66 live tables, **exactly one** orphan, and it is `system_config`. All 7 live RPC functions trace to committed migrations. The concern is closed by evidence. Stated caveat rather than buried it: the diff covers the `public` schema's tables and exposed functions, not indexes, triggers, or policies. | ecdba21 |

### Contract 43 — 2026-08-02 — D-613 All Pending Gates Scope

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-43-A | Division-Leader scope becomes `isLeadershipForCycle`, resolved once per Division | CC-40-P scoped the screen to directly-owned divisions while D-577 grants a parent Division Leader approval authority over child divisions via the ancestor walk — leaders held power over work the screen never showed them. Now one scope function, not two. Resolved once per **distinct Division** rather than per gate, because the walk would otherwise repeat for every gate in the same Division. The queries sit inside the `!isWide` branch deliberately, so no FIFO fixture in the IE/Admin/Phil tests shifts (Rule 40). Kept the `owner_user_id` query as the access gate — owning no Division still means the screen is not yours — and verified the Contract 41 ordering survives: `delivery_cycle_id` still narrows `gatesQuery` before the division filter, so a leader passing a foreign cycle id gets nothing. | ecdba21 |
| CC-43-B | `owns_division` is derived in the existing profile call, never stored | D-613 forbids an `is_division_leader` flag, and the reason generalises: a stored flag duplicates a fact `divisions.owner_user_id` already owns and drifts the moment ownership changes, silently costing someone access to their own queue. Folded a single batch query over `owner_user_id` into `list_users` — the call the app already makes for the profile — so no round trip was added. Chose `list_users` over a new tool because the sidebar reads its result already. | ecdba21 |
| CC-43-C | The CC-40-P source assertion is updated, not deleted | `contract40-reassign.test.js` asserted the old `ownedDivisionIds` filter as source text. D-613 supersedes that behaviour, so the assertion is now false by design. Rewrote it to pin the new scope (`leadershipDivisionIds`, `isLeadershipForCycle`) rather than removing it — the test's value is catching a silent revert, and that value survives the supersession. | ecdba21 |

### Contract 44 — 2026-08-02 — Notification Triggers (reconciled to as-built)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-44-A | One shared `notifyGateReturned`, closing the silent L2/L3 path | V2 found the real defect: the L1 consensus return notified the trio, and the single-approver return path — every Level 2 and Level 3 return, the majority of returns — sent **nothing**. One event, two implementations, one of them empty. Fixed by giving both paths a single helper rather than copying the L1 block, because a copy is what produced the divergence. Recipients are submitter + all trio minus the actor (D-345): the submitter was added because an Admin submitting on behalf was never told their own submission came back. "Return with Set Conditions" rides in the same email with the condition count rather than sending a second — two messages about one action is noise. `submitted_by_user_id` was added to the **existing** `gate_records` select, not a new query, so no FIFO slot shifts (Rule 40). | ecdba21 |
| CC-44-B | Contract 44's V1 premise does not hold; wording fixed, recipients unchanged | The spec states that at Level 1 "the trio members whose turn it actually is are not on the recipient list at all." That is false against the code: `deriveConsultedUserIdsV2` pushes the non-null trio into the Consulted set before any C stakes, so every remaining collected party is already addressed. I had begun adding the trio explicitly and reverted it — it was dead code the helper's dedupe would have absorbed, and shipping redundant code to satisfy a stale premise is worse than shipping none. What WAS wrong is the copy: at L1 the body read "You have been notified as an approver or a consulted party," which misdescribes consensus. It now states that the gate passes when every collected party approves. Added a regression test pinning the trio's presence so the derivation cannot silently drop them later. | ecdba21 |
| CC-44-C | The remaining nine trigger rows were verified, not rebuilt | Contract 44 says the trigger list "has exactly two entries." Fourteen `email_type`s are live, including rows the spec asks to build — `ie_override`, `governance_level_lowered`, `approved_over_returned_consultation`, `consulted_removed`, `cancel_requested`, `cycle_cancelled`, `post_approval_decline`, `approver_override`, `informed_gate_decision`. Building them as specified would have produced duplicate sends. Phil authorised fixing Contract 44 in-session rather than routing to Design, so scope was set to the genuine gaps only; the rest are reported as as-built for Design to reconcile the inventory against. | ecdba21 |

### Contract 45 — 2026-08-02 — Units A and B (manager relation, "My team")

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-A | The D-638 FK references `users(id)`, not `users(user_id)` | The spec specifies `REFERENCES users(user_id)`. That column does not exist — `public.users`' primary key is `id`, read from the live schema on 2026-08-02. As written the migration fails at execution. Corrected in migration 098 and flagged in the file header rather than fixed silently, because the same wrong column name appears in the Contract 45 spec text Design will reuse for the queue work. | 61ef28d |
| CC-45-B | Reporting-loop prevention is an upward chain walk with a depth guard | A self-referencing FK does not prevent A→B→A — Postgres accepts it, and every consumer of the relation then walks a chain that never terminates. So the guard has to be application-layer, on write. Walks upward from the PROPOSED manager looking for the edited user; a parent-only check would miss the two-hop case, which the tests cover explicitly. Depth-bounded at 50 so a loop already present in the data cannot hang the very request that is checking for loops. Error names the person in the way, per D-140. | 61ef28d |
| CC-45-C | Manager is a plain select, not an S-022 EntityPicker | S-022 reserves pickers for entities with attributes that disambiguate the choice, and dropdowns for short flat lists of scalar values. A manager is one colleague chosen by name — no scope radio, no status badges, nothing to disambiguate. Self is excluded from the options so the commonest mistake is simply unavailable; deeper loops cannot be detected client-side without walking the chain, so the server rejects those. | 61ef28d |
| CC-45-D | D-648's widening is NOT implemented — "My team" intersects with existing Division access | D-648 requires "My team" with no explicit Division choice to widen to all Divisions. `list_delivery_cycles.js:104-109` restricts every non-privileged caller to their Division memberships before any client-side filter runs, so honouring it literally means bypassing a data-access boundary for non-privileged users. Rule 30 withholds autonomy on security boundaries and D-381 makes access questions flag-and-stop, so this went to Phil rather than being decided in code. Phil ruled 2026-08-02: ship the intersection, route the widening to Design. Note this also refines my V3 answer — Division scope is enforced in the MCP application layer, not RLS, so V3 asked about the wrong layer. | 61ef28d |
| CC-45-E | Gate Schedule gets a toggle, not a person filter | §7 says "add an option to the Person/Submitter filter" on four surfaces. Gate Schedule has no person filter — its controls are a Divisions toggle and a gate select. Building a filter panel there to host one option would be a larger change than the section asks for and would leave the screen inconsistent with itself. "My team" mirrors the existing "Display only my Divisions" toggle beside it. Recorded as a deviation under Rule 7. | 61ef28d |
| CC-45-F | One `MyTeamService`, resolving direct reports once, failing closed | "My team" now appears on four surfaces; defining it four times is how the two All-Pending-Gates scopes diverged in Contract 43. Defined once (`manager_user_id = current_user`, direct reports only, no transitive walk per D-639) and cached for the session, since the reporting line only changes through the Admin panel. On any failure it resolves to an EMPTY set, not an unfiltered result: an empty "My team" reads as "no team", which is honest, whereas falling through would silently show more than the user asked for. | 61ef28d |
| CC-45-G | On Initiative Activity, "My team" stages reports into the existing multi-select | That screen's person filter is already a multi-select of user ids, so "My team" needs no new filter dimension — it fills in the one that exists. Made additive rather than replacing the selection: a manager comparing their team against a peer has staged that peer deliberately, and clobbering it would undo their work. The button toggles, so it also clears cleanly. | 61ef28d |

### Contract 45 — 2026-08-03 — Unit C (notification queue helper)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-H | Delivery class is decided by the CALLER; the loud four are decided by the helper | D-641's rule is "in the waiting-on line → immediate, awareness party → digest." `computeWaitingOnBatch` returns a rendered line and a state, **not** the set of user ids it names, so the helper cannot test membership without reimplementing that logic and letting the two drift. The call site already knows why it is addressing each person — approver, trio, consulted, informed — so it states the class. The four loud exceptions are deliberately NOT left to callers: "never suppressible" is precisely the property that must not depend on remembering to pass a flag, so they are forced immediate from one frozen list inside the helper. Unspecified class defaults to **digest**, because an awareness message sent immediately is noise while the blocking class is always stated explicitly. | a96ee88 |
| CC-45-I | The helper ships inert, ahead of the call-site conversion | Unit C converts 14 `email_type` call sites off direct Edge Function invocation, and a partially-converted path sends **nothing at all** — strictly worse than the pre-Contract-44 silence, because the trigger looks present. Committing the helper first, with nothing calling it, means every stop point is safe: behaviour is byte-identical to today until the conversion runs as one pass. Deliberate sequencing, not an incomplete unit. | a96ee88 |
| CC-45-J | Dedupe by user id, immediate wins; manager copies always batch | A person holding two stakes (trio and Informed) is one person and gets one row — the stronger obligation governs, so immediate beats digest. A manager who is already a direct recipient gets no second copy. Manager fan-out rows are always `digest` even when the report's own row was immediate: a manager's copy is awareness by definition, and making it immediate would recreate the volume problem D-643 exists to solve. | a96ee88 |

### Contract 45 — 2026-08-03 — Unit C part 2 (call-site conversion)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-K | All 14 call sites converted in one pass; zero direct senders remain | D-642's test is "no MCP tool invokes `send-notification-email` directly", and `grep` over `src/tools/` now returns nothing outside the two helpers. Done as a single pass rather than incrementally: a mixed estate is the state where nobody can answer "is this event queued?" without reading every file. Only ONE existing test needed a fixture change across the whole conversion (`contract39-cast-outcome`, two slots for the manager lookup and the insert), which is the evidence that immediate-class behaviour is genuinely unchanged. | e409819 |
| CC-45-L | D-647's Informed reclassification is HELD at immediate until the digest exists | D-647 routes Informed gate decisions to the digest. Setting `digest` now — with no 06:00 job — would send Informed parties **nothing at all**, and do it invisibly, because the row would sit unsent in the queue looking healthy. That is worse than the pre-Contract-45 behaviour and would not surface until someone noticed silence. The row is queued either way; only the class flips, and it flips in the same commit that ships Unit D. The same hold applies to `close_review_assessment_roster`. Marked in code at both sites so the flip is a one-line change, not a rediscovery. | e409819 |
| CC-45-M | A queue-write failure must not silence an immediate send | Found while converting: the helper returned early on insert error, so a `notification_queue` problem would have swallowed a blocking notification. Before Contract 45 the email went out with no queue involved at all, so that would have been a regression introduced by plumbing rather than by any decision. The helper now logs and carries on to dispatch. Digest rows are lost in that case — the lesser failure, since they are awareness and the error is in the log. Regression test added. | e409819 |
| CC-45-N | Two events classed immediate that are arguably awareness | `dl_override_notification` (D-569(5)) is not one of the frozen four, but it is the escalation half of a loud event — batching it while the override itself went out at once would be incoherent. `close_review_assessment_roster` is the closing artefact of an Initiative and reads as a record people keep; folding it into a five-line digest section would lose the content. Both are flagged here rather than buried, because the SOF Appendix A inventory that would settle them travels in the companion document, which was not in the zip. | e409819 |

### Contract 45 — 2026-08-03 — Unit D part 1 (digest assembly)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-O | Migration 099 adds `manager_copy` rather than deriving it from the headline | D-643's sections 1 and 4 — "Waiting on you" and "Waiting on your team" — are the SAME event_type distinguished only by whether the row is the participant's own notification or the D-642 manager copy. `notification_queue` as created in 098 cannot answer that. The fan-out is detectable today only by the `"Name — "` prefix the helper writes, which is presentation text: it would work until the first person reworded a headline, and then manager copies would silently file under "Waiting on you". A boolean defaulting false needs no backfill and cannot drift. | 1c3ce0c |
| CC-45-P | The event→section mapping is Code's, and is one table so Design can correct it cheaply | D-643 names the ten sections but the event→section inventory lives in SOF Appendix A, which travels in the companion document — not supplied with this contract (D-571 says it should have been). Rather than stall Unit D, the mapping is reasoned from each event's meaning and isolated in a single frozen `SECTION_BY_EVENT` object. Unmapped events fall back to "Moving" rather than being dropped, so a future event type is mis-filed rather than silently lost. | 1c3ce0c |
| CC-45-Q | Digest assembly is pure, with no Supabase in it | Almost everything D-643 specifies is judgement — ordering, omission, the five-line cap, what the subject may say — and almost none of it is I/O. Keeping `lib/digest.js` free of the database makes those rules testable directly, which is why the D-568 constraints (no per-person comparison, section names describe work not people) could be pinned as assertions rather than left as prose. | 1c3ce0c |
| CC-45-R | Subject counts include overflow items, and never name a person | The subject reports what is true, not what fits in five lines — "9 blocked" when four are behind an overflow link. It also never interpolates a name: D-568 forbids per-person comparison, and the subject is the most quoted, most forwarded part of an email. People appear in the lines, which are facts about work. | 1c3ce0c |

### Contract 45 — 2026-08-03 — Unit D part 2 (digest job + scheduled-caller auth)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-S | The digest job lives in the MCP because the MCP survives the port | ARCH-36-SCHED asked which mechanism to use. Decided on resilience, per Phil 2026-08-03. CLAUDE.md's porting contract and D-263 say TRIARQ engineers replace the **infrastructure layer** at port while "the Angular application, MCP server tool contracts, and skills layer are unchanged" — and Supabase Edge Functions and pg_cron ARE that layer. Putting D-643's judgement (ten sections, five-line cap, suppression, subject rules) into an Edge Function or plpgsql would mean re-implementing all of it at port, in a different stack, from a spec whose companion document never arrived. Here only the trigger is disposable: one cron entry pointing at one route. pg_cron is confirmed available and in production since Contract 32 (migration 054) — it was rejected on portability, not capability. `pg_net` was NOT verified and is not depended on. | 4e52504 |
| CC-45-T | `RENDER_INTERNAL_API_KEY` implemented as a narrow door, not a second credential on the tool router | The 06:00 job has no user, and Arch-5 requires a JWT on every tool call — correctly, because a JWT belongs to a person. Rather than mint a service-account user row (a real identity with real permissions, usable for anything), scheduled work gets `/internal/*` routes mounted BEFORE `validateJwt`, each exposing exactly one operation. The rejected shortcut was accepting the key inside `app.use(validateJwt)` as an alternative credential: that would make ~90 tools reachable with one static string. This key cannot reach `/tools/:toolName` at all. Arch-4 has declared this env var since Build C; nothing had implemented it. | 4e52504 |
| CC-45-U | An unset key DISABLES the route rather than opening it | The dangerous default for `if (!expected)` is to skip the check. The route 404s when `RENDER_INTERNAL_API_KEY` is absent, so a missing or mistyped Render env var yields "no such endpoint" rather than an unauthenticated job trigger. Comparison is `timingSafeEqual` — a static long-lived credential should not leak a character at a time to anyone who can measure response times. Both pinned by test. | 4e52504 |
| CC-45-V | Only `blocked` and `at_risk` are re-checked before send; everything else is historical | D-643 requires suppressing state lines whose condition has resolved, and explicitly exempts completed and moved lines. Implemented as a frozen `STATE_EVENT_TYPES` list rather than a per-event flag, so the exemption is visible in one place. When the verification lookup itself fails the row is SENT, not suppressed: an unsuppressed true line is a smaller error than silently dropping a blocked Initiative because a query failed. Rows are stamped as they are processed and the claim query reads only unstamped rows, so a double cron fire sends nothing twice. | 4e52504 |

### Contract 45 — 2026-08-03 — Unit D part 3 (key naming corrected to house pattern)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-W | Per-job key `DELIVERY_DIGEST_INTERNAL_CRON_KEY`, not the generic Arch-4 one | CORRECTS CC-45-T. Phil flagged the generic name; the precedent he remembered is real and I had missed it. Contract 38 already built this exact pattern in team-meetings-mcp — `/internal/send_meeting_reminders`, header `x-internal-key`, key `TEAM_MEETINGS_INTERNAL_CRON_KEY`, with the code comment recording "named specifically per Phil 2026-07-20 — one purpose, one key." My earlier claim that nothing had implemented scheduled-caller auth was wrong: I grepped for `RENDER_INTERNAL_API_KEY` and `INTERNAL_API_KEY`, which does not match that name. Renamed the key, switched the header to `x-internal-key`, and renamed the route to `/internal/run_daily_digest` (underscores) so both services read identically. The security argument for per-job keys stands on its own: one shared secret means rotating any job rotates them all, and a leak from one exposes the rest. | 430013b |
| CC-45-X | Migration 100 schedules via pg_cron + pg_net, matching migration 078 | ALSO CORRECTS an earlier statement: I reported pg_net as unverified. It is in production — migration 078 runs `CREATE EXTENSION IF NOT EXISTS pg_net` and `net.http_post` on a 30-minute heartbeat. So the trigger uses the house pattern rather than a Render Cron Job, and no new infrastructure concept is introduced. The CC-45-S reasoning is unaffected and in fact cleaner: the digest LOGIC stays in the MCP because the MCP survives the port, while the trigger is one `cron.schedule` entry that the port replaces along with the rest of the infrastructure layer. | 430013b |
| CC-45-Y | Scheduled at 10:00 UTC, which is 05:00 ET in winter — flagged, not hidden | pg_cron schedules are UTC. D-643 names 06:00 ET as a system constant, and no fixed UTC hour honours that across DST. Rejected two alternatives: paired entries swapped at the DST boundary (silently wrong the first time the swap is forgotten) and a job that checks local time and no-ops (a job that usually does nothing has invisible failures). An hour early for five months is the smallest honest error, is reversible in one line, and is recorded in the migration header rather than left for someone to discover. | 430013b |

### Contract 45 — 2026-08-03 — Unit D part 3 (commitment checks + D-647 flip)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-Z | `STALE_COMMITMENT_DAYS = 14`, Code's choice | D-649 fixes 42 days for weak commitment but leaves stale as "N days". Chose 14: just outside a fortnightly rhythm, so a team on a two-week cadence is never flagged for working normally, while a month of silence is caught twice. Both constants are named rather than inlined, per the ARCH-33 pattern, so the eventual admin surface has an obvious seam. | d911bdb |
| CC-45-AA | The weekly repeat window is enforced against the queue, not a new table | D-643: "state lines appear on entry, then weekly." Commitment findings are state — an Initiative with no target date has no target date again tomorrow, and re-queueing daily would put the same three lines in someone's digest every morning until they act, which teaches people to skip the section. Enforced by checking `notification_queue` for a row of the same event_type + recipient + Initiative inside 7 days. The queue is already the durable record of what was said to whom; a separate "last emitted" table would be a second bookkeeping surface to keep in step. | d911bdb |
| CC-45-AB | Calendar-day comparison for the 42-day boundary — a real off-by-one found by test | `target_date` is a SQL DATE (midnight); `now` is a timestamp. Comparing them directly floored away part of a day, so a gate targeted 43 calendar days out measured as 42 and the weak-commitment check missed it by one. Fixed by normalising `now` to midnight UTC. Staleness deliberately does NOT use that normalisation: "not updated in N days" is a genuine elapsed-time question, and truncating it would report a change made an hour ago as a full day old. Two adjacent checks, two different correct treatments of time. | d911bdb |
| CC-45-AC | Commitment checks address the trio only; managers arrive by fan-out | D-649 says "to the trio and each trio member's manager". Implemented as trio-only recipients, letting the D-642 fan-out add the managers. Resolving managers inside the checks would have produced two rows per manager — one direct, one fanned out — and the duplicate would only surface in someone's digest. Also: an Initiative with no trio assigned is skipped rather than escalated; there is nobody the check is about. | d911bdb |
| CC-45-AD | D-647 flip completed for Informed decisions; the Close Review roster deliberately NOT flipped | Informed gate decisions now write `digest` class. Informed is an awareness stake — never waited on, never in the D-565(4) waiting-on line — which is exactly the D-641 test. Held at immediate until the 06:00 job existed, because `digest` with no job would have sent those parties nothing at all, invisibly. `close_review_assessment_roster` stays immediate against the same decision: it is a multi-line record of who assessed what, and the digest caps sections at five single-line headlines, so folding it in would truncate the content the email exists to deliver. Flagged for Design rather than assumed. Stake removal and cancellation stay immediate per D-647 itself. | d911bdb |

### Contract 45 — 2026-08-03 — Unit D part 4 (dry-run reports intent)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-AE | A dry run reports the commitment findings it WOULD write | Found by running the real dry run against production: `commitment_checks_written` read 0 regardless, because the counter incremented only after the write. That makes a dry run useless for its one purpose — previewing volume before anything sends — and it made the two halves of the job disagree, since the digest path already reports `sent` as what it would send. Counter moved ahead of the dry-run guard. Four tests added covering intent-reporting, the no-trio skip, the weekly-window skip, and that a check failure still lets the queued digest go out. | 59e6ebd |
| CC-45-AF | The digest-job test mock was missing `.gte`, and the non-fatal catch hid it | The new tests failed with zero findings while the pure logic was demonstrably correct. Cause: `writeCommitmentChecks` calls `.gte('created_at', …)` for the weekly window, the FIFO mock chain had no `gte`, and the resulting TypeError was swallowed by the deliberately non-fatal catch. Worth recording because the catch is correct — a commitment-check failure must not stop the digest — but it means this function's failures are invisible in tests unless the mock is complete. The console.error is the only signal in production too. | 59e6ebd |

### Contract 45 — 2026-08-03 — Unit E deferred (D-644)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-45-AG | Unit E (D-644 notification preference) DEFERRED — Design ratification required | Phil's call 2026-08-03. **This is a deferral of a LOCKED decision and Code does not have authority to make it stand — it is recorded here for Design to ratify or overturn.** Why it defers safely: D-644 sets the default to `daily`, so with no toggle every user sits on daily for the awareness class, which is exactly where the toggle would have placed them. The deferral therefore cannot over-mail anyone; the only lost capability is a user who WANTS immediate awareness mail asking for it. The blocking class and the four loud exceptions were never preference-governed, so nothing urgent is affected. Nothing is half-built: no `notification_preference` column, no code reading one, no surface stub — a clean line rather than a seam. **Contract 45's Definition of Done line "One preference toggle; immediate class unaffected by it" is therefore NOT MET and is declared unmet in the CodeClose.** Two blockers also stand behind this deferral and remain open for Design: (1) D-169's text is not in this repository — it lives in `decisions-active.md`, Design-side — so the decision governing what a user may change about themselves could not be read, and Unit E would have been the first self-service write in the system; (2) the "D-169 preference surface" the spec names does not exist — `angular/src/app/features/` has no settings, profile, or preferences route, so it is a new surface however it is built. Code's lean, recorded for whenever Unit E resumes: put the toggle in the existing My Notifications Home card (universal, position fixed by D-425, already where a user looks when thinking about notifications, no new sidebar item against S-026), and treat a self-service User panel reached from the sidebar-footer name — the S-007 reuse answer — as its own contract once there is more than one preference to hold. | 1a8e3e2 |

### 2026-08-04 — Initiatives grid filter visibility (follow-on, no contract number)

Date-based CC numbering per the CC-0714 / CC-0726 precedent — this is a defect fix Phil reported from live use, not a Design-specified contract.

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-0804-01 | Chips for the four drill-in filters — DCS, EPO, DOL, Next Gate | Phil reported the grid "broken": Practice Services showed 0 of 59. Root cause was `filterDcs` set to a specific DCS, applied at line 2148, **persisted** to `user_screen_state`, restored on every visit, and represented **nowhere** on screen — no chip, not in the Filters badge, no panel row. A drill-in from a role view (`qp['dcs']`, CC-38-40) had silently narrowed his grid indefinitely. S-012 requires every active filter to render as a dismissible chip and D-297 says inform rather than hide; both were violated for four filters. Chips added following the existing pattern exactly, with immediate re-query on dismissal per S-012. `filterEpo`, `filterDol`, `filterNextGate` had the identical defect and are fixed together — fixing only the one that bit would leave three loaded guns. | 45cb434 |
| CC-0804-02 | The Filters badge counts them | `activeFilterCount` omitted all four, so Phil's badge read **1** when two filters were narrowing the grid. The badge is the only summary of how constrained a view is; a badge that undercounts is worse than no badge, because it actively reassures. | 45cb434 |
| CC-0804-03 | Clear All clears everything, including filters with no panel row | Phil: "make Clear All always clear everything." The four drill-in filters have no staged twin because they have no panel row, so `clearStagedFilters` could not reach them — Clear All followed by Apply left them untouched, which is why the filter survived every attempt to clear it. They are now cleared directly. **S-011 is preserved**: no query is fired and the panel stays open; the grid repaints when Apply commits. Flagged rather than assumed — making Clear All fire its own query would have been an S-011 deviation needing a decision, so it does not. | 45cb434 |
| CC-0804-04 | Clear All no longer reads as disabled | Phil reported the button as disabled; it never was — no `[disabled]` binding exists. `#D0D0D0` border with `#5A5A5A` text was D-198's recessed-secondary treatment pushed far enough that a live control looked inert, which cost a real support round trip. Now an outline button in `--triarq-color-primary`: still visibly secondary against the filled Apply, unmistakably enabled. | 45cb434 |
| CC-0804-05 | "0 of 59" was CORRECT, and there is no load-ordering bug | Recorded because I suspected one and said so. 59 = Practice Services (1) + QSuite & Pathways Clinical (27) + Revenue Cycle Management (31), because Phil's saved state carries `includeChildDivisions: true` and the server correctly expands to descendants. The client then filtered 59 → 0 on the invisible DCS filter (Christian Sherbinow, assigned to none of them). The 2026-07-25 race fix is intact and was not implicated. Verified against the live database rather than reasoned about — and my two earlier hypotheses (an Interest profile, duplicate `user_screen_state` rows) were both checked and both wrong: `interestConditions` is `[]`, and 210 rows map to 210 distinct (user, screen_key) pairs. | 45cb434 |
| CC-0804-06 | Pattern sweep: the defect is confined to this one grid | Searched every component reading `qp['dcs'] / ['epo'] / ['dol'] / ['next_gate']`. Only `delivery-cycle-dashboard.component.ts` both reads those params and persists them, so no sibling grid carries an invisible persisted filter. Stated because "we fixed it here" is worth nothing without knowing where else it lives. | 45cb434 |

### 2026-08-04 — Admins selectable as Division Approvers (AMENDS D-600)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-0804-07 | Admins are eligible to be DESIGNATED Division Approvers, without membership — **amends D-600 / CC-40-R** | **This amends a LOCKED decision and requires Design ratification.** CC-40-R specified "members-only (target must have an active `division_memberships` row)" and was ratified as D-600. Phil 2026-08-04: "I do not want admins automatically available for all initiatives — but they should be options to be configured as Division Approvers (or Division Owners)." Flagged before implementing; built under his direction. Why the amendment is coherent rather than a loosening: **D-170 already grants Admins implicit access to every Division**, so members-only forced an Admin to be enrolled as a "member" of a Division purely to satisfy a check — recording a false organisational fact to pass a gate, and leaving anyone reading a Division's member list unable to tell who works there from who is there for plumbing. Admins were the one population with standing everywhere and eligibility nowhere. What deliberately does NOT change: `list_eligible_approvers` is untouched, so Admin is **not** a source of automatic eligibility and being an Admin still puts nobody on any Initiative's approver dropdown. Only the `division_approvers` row confers that, exactly as for any other approver, and CC-40-R's "picker-only" lock over D-557 automatic resolution still holds. Enforced on both sides: `set_division_approver` skips the membership check for `is_admin` targets; the Angular picker unions members with active Admins. Two tests added — the admin-target test supplies **no** membership fixture, so the membership query's absence is the assertion. | 6cf2546 |
| CC-0804-08 | Division Owner needed no change — already all-users | Phil's request covered "Division Approvers (or Division Owners)". Checked before building: the Division Leader picker already passes `[allUsers]="true"` and is Phil-only to edit (D-471), so any user including a non-member Admin can already be assigned. No change made. Recorded so the second half of the request is visibly answered rather than silently dropped. | 6cf2546 |
| CC-0804-09 | Practice Services has no Division Leader — flagged, not fixed | Found while tracing why Craig was ineligible. `divisions.owner_user_id` is NULL for Practice Services, a Trust with 27 members and two child Service Lines holding 58 Initiatives. Every RCM and QSuite Initiative therefore loses the parent-leader rung of the D-577 ancestor walk — `list_eligible_approvers` runs the walk and finds nobody. Not a defect in code; a gap in configuration with governance consequences. Assigning a Division Leader is Phil-only under D-471, so it is surfaced rather than actioned. | 6cf2546 |

### 2026-08-04 — Phil can skip the Deploy gate (completes the 2026-07-24 override)

| CC-letter | Title | Reasoning | Commit |
|---|---|---|---|
| CC-0804-10 | `confirm_gate_skip` now honours `phil_override` on the D-450 deploy block | Phil hit `DEPLOY_GATE_SKIP_BLOCKED` trying to close an Initiative. Root cause: the two halves of a two-call flow disagreed. `submit_gate_for_approval:262` has relaxed the block for Phil since 2026-07-24 and says so in terms — *"Phil override: the deploy-skip block relaxes to the normal skip confirmation — Phil may skip any gate, Deploy included"* — while `confirm_gate_skip:84`, the call that actually performs the skip, applied it absolutely. So submit offered the skip interstitial, the user accepted, and the delegate refused. `phil_override` was already being forwarded correctly (Rule 45 / D-596 satisfied); this one check simply ignored it. Treated as **completing an incomplete implementation of the 2026-07-24 override rather than amending D-450** — the intent was already established in the sibling tool and documented in its comment. The caller is re-verified via `isPhil` rather than trusted from the parameter, exactly as submit does. **D-450 still holds for everyone else**, and being Phil is not enough on its own — the override must be deliberately armed. Flagged for Design regardless, since it touches a locked decision. | f6eaff3 |
| CC-0804-11 | The override had no UI path from the state that blocks you | `"Submit anyway (override)…"` renders only inside the `hardStops` block, and a deploy-skip block is not a hard stop — so from the screen Phil was actually on, there was no control to arm the override. The backend permission existed and was unreachable, which is the worst combination: the capability is real, the user is entitled to it, and nothing on screen admits it exists. Added a Phil-only `"Skip anyway (override)…"` to the deploy-blocked panel. | f6eaff3 |
| CC-0804-12 | The raw error code was being shown to the user | `onConfirmSkip` funnelled the failure through `endProcessing(res.error)`, so the literal string `DEPLOY_GATE_SKIP_BLOCKED` appeared in the interstitial above a Skip & Submit button that could never succeed — a D-140 violation twice over: an error code as user-facing text, and a blocked action with no statement of what would unblock it. Now routed to the existing explanatory `deploy-blocked` state, which lists the gates needing action and names backdating as the way through. The server message was also rewritten from *"confirm_gate_skip rejects any gates_to_skip array that includes go_to_deploy"* — which described the implementation to the user — to the backdating instruction. | f6eaff3 |
| CC-0804-13 | Behavioural tests, because the existing coverage could not have caught this | `tools.test.js` tests the deploy block by grepping the source for the error string, so it passed throughout the entire period the flow was broken. Five behavioural tests added: non-Phil still refused, a forged `phil_override` without being Phil refused, Phil with the override passing, Phil WITHOUT the flag still blocked, and the refusal naming the way through. The forged-flag test matters most — it pins that the parameter is not trusted. | f6eaff3 |

---

*TRIARQ Health | Pathways OI Trust | CONFIDENTIAL*
